NVIDIA_API_KEY=your_nvidia_api_key_here

# Optional: override the default LLM model.
# Run `uv run python -m src.list_models` to see what's available on your account.
# Models marked ✓ support parallel tool calls (recommended).
# NVIDIA_MODEL=meta/llama-3.3-70b-instruct
